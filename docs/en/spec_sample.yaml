# Specification Sample (YAML Format)
# validator_path: spec_validator.yaml
---
document_info:
  title: "Sample App Spec"
  version: "1.0.0"
  created_date: "2025-04-29"
  author: "Your Name"

overview:
  purpose: "This app provides a minimal Windsurf demo."

requirements:
  functional:
    - User can register
    - User can login

api_specification:
  endpoints:
    - POST /api/register
    - POST /api/login
  response_examples:
    - /api/login: { "200": "Login success", "401": "Auth failed" }
